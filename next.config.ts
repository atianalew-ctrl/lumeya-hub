import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Allow iframes from Vercel sites
  async headers() {
    return [
      {
        source: "/(.*)",
        headers: [
          {
            key: "X-Frame-Options",
            value: "SAMEORIGIN",
          },
        ],
      },
    ];
  },
};

export default nextConfig;
